#Q19 What happens when „1‟== 1 is executed?

"""When '1'==1 is executed in Python, it will return False. This is because '1' is a string and 1 is an integer. In Python, 
strings and integers are not equal, even if they have the same value. To compare two strings in Python, you can use the == operator. 
However, to compare a string and an integer, you need to use the == operator. The == operator will convert the string to an integer before comparing it to the integer.
For example, the following code will print True:"""

print('1' == '1')